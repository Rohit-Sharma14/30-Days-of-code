//default params
const leadSinger = (artist = "someone") => {
    console.log(`${artist} is the lead singer of Cold Play`);
}

leadSinger();// if here no params are given then artist will get the value of someone


//challenge
const grocery = (food = "something") => {
    console.log(`i am going to buy ${food} from market`);
}

leadSinger();